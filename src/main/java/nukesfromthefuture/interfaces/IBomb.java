package nukesfromthefuture.interfaces;

import net.minecraft.world.World;

public interface IBomb {
	void explode(World world, int x, int y, int z);
	//yes, that's it, just one line but two including me... but 6 including all OwO
	//interfaces  r useless and r just an excuse for a programmer to not write code but still have it on the file. 

}
